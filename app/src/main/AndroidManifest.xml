<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="pk.gov.pbs.tds_example.CustomApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity android:name=".activities.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.PrimaryFormActivity" android:exported="false"/>
        <activity android:name=".activities.SecondaryFormActivity" android:exported="false"/>
        <activity android:name=".activities.TertiaryFormActivity" android:exported="false" />
        <activity android:name=".activities.S1AActivity" android:exported="false"/>
        <activity android:name=".activities.JsonBasedFormActivity" android:exported="false" />
        <activity android:name=".activities.JsonBasedForm2Activity" android:exported="false" />

    </application>

</manifest>